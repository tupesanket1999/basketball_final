package com.example.basketballcounterapp;

/*
import android.util.Log;

import com.google.firebase.database.DataSnapshot;
import com.google.firebase.database.DatabaseError;
import com.google.firebase.database.DatabaseReference;
import com.google.firebase.database.FirebaseDatabase;
import com.google.firebase.database.ValueEventListener;

public class Data_match{



    public void write(){

        // Write a message to the database


       // myRef.setValue("Hello, World!");
    }



    Data_match(int match){

        teamA = MainActivity.s1;
        getPlayer();
        teamB = MainActivity.s2;
        getPlayer1();
        match_number = match;
    }

    int match_number;
    String teamA;
    String teamB;

    public void getPlayer(){
        for (int i =0; i < 12 ; i++) {
            Player[] player = new Player[12];
            player[i] = new Player();
            player[i].playername = Player_info.player_info[i].name;

            player[i].free = Player_info.player_info[i].Free_throws;
            player[i].three = Player_info.player_info[i].Three_pointer;
            player[i].two = Player_info.player_info[i].Two_points;
            player[i].foul = Player_info.player_info[i].foul;
        }
    }
    public void getPlayer1(){
        for (int i =0; i < 12 ; i++) {
            Player[] player = new Player[12];
            player[i] = new Player();
            player[i].playername = Player_info.player_info1[i].name;

            player[i].free = Player_info.player_info1[i].Free_throws;
            player[i].three = Player_info.player_info1[i].Three_pointer;
            player[i].two = Player_info.player_info1[i].Two_points;
            player[i].foul = Player_info.player_info1[i].foul;
        }
    }

    public class Player{

        String playername="";
        int three=0;
        int two=0;
        int free=0;
        int foul=0;
    }
}
*/